<h1 align="center">Articles</h1>

<h3 align="center">Our blog has <%= Article.public_count %> articles and counting!<h3>

<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li>
        <%= link_to article.title, article %>
      </li>
    <% end %>
  <% end %>
</ul>

<Button> <%= link_to "Add New Article", new_article_path %><Button>
